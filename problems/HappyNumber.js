const testNumber = 19;

var isHappy = function (n) {
	let numberArray = numberArray.toString().split("");
	let counter = 0;
	while (true) {
		Math.pow(Number(numberArray[counter - 1]), 2);
	}
};
isHappy(291);
